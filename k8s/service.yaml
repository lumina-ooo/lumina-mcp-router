apiVersion: v1
kind: Service
metadata:
  name: lumina-mcp-router
  namespace: mcp-server
  labels:
    app: lumina-mcp-router
spec:
  type: NodePort
  selector:
    app: lumina-mcp-router
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      nodePort: 30210
      protocol: TCP
